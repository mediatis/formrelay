plugin.tx_formrelay {
	settings{
		# cat=plugin.tx_formrelay//1; type=int+; label= Enable: Only convert to XML if not set to 0.
	    enabled = 1
	    opcode =
	    system = live
	    logfile_base_path =
	}
}
