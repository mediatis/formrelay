TYPO3:
  CMS:
    Form:
      prototypes:
        standard:
          finishersDefinition:
            Formrelay:
              FormEngine:
                label: 'Formrelay'
                elements:
                  setup:
                    label: 'Setup'
                    config:
                      type: 'text'
                  baseUploadPath:
                    label: 'Base upload path'
                    config:
                      type: 'text'
                  addHiddenFields:
                    label: 'Add / set hidden field values'
                    config:
                      type: 'text'
